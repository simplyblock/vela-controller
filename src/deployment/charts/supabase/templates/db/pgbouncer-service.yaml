{{- if .Values.pgbouncer.enabled -}}
{{- $pgbouncerName := printf "%s-pgbouncer" (include "supabase.fullname" .) -}}
{{- $pgbouncerSelectorName := printf "%s-pgbouncer" (include "supabase.name" .) -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ $pgbouncerName }}
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.pgbouncer.service.type }}
  ports:
    - port: {{ .Values.pgbouncer.service.port }}
      targetPort: 6432
      protocol: TCP
      name: pgbouncer
  selector:
    vm.kubevirt.io/name: {{ include "supabase.db.fullname" . }}
{{- end }}
